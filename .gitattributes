# Normalize all text files to LF in the repository
* text=auto eol=lf

# Ensure shell scripts use LF (even on Windows checkout)
*.sh text eol=lf

# Binary files
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
